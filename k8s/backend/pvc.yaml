apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aqconnecta-uploads-pvc
  namespace: aqconnecta
  labels:
    app: aqconnecta-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: microk8s-hostpath
  volumeMode: Filesystem
