apiVersion: v1
kind: Service
metadata:
  name: aqconnecta-frontend
  namespace: aqconnecta
  labels:
    app: aqconnecta-frontend
spec:
  type: ClusterIP
  selector:
    app: aqconnecta-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
